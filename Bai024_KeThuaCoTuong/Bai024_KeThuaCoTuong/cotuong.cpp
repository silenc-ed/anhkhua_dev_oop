#include "cotuong.h"

toado nhap(toado a) {
	cin >> a.x >> a.y;
	return a;
}

void newboard(toado a) {
	quanco b[10][9];
	nothing a;
	for (int i = 0; i < 10; i++) {
		for (int j = 0; j < 9; j++) {
			b[i][j] = a;
		}
	}

}

toado TUONG::dichuyen() {
	
}
